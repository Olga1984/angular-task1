# WK Version String

```javascript
var vstring = require('@wk/version-string');
console.log(vstring.releaseVersion); // Bamboo Hub doesn't support things like 1.2.0-beta.x or 1.2.x-beta.0, so this string won’t have an x in it
console.log(vstring.xVersion); // Something like 1.2.x
```
