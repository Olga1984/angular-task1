'use strict';

let fs = require('fs');
let path = require('path');
let Sassaby = require('sassaby');

describe('_wk-init.scss', () => {
    let file = path.resolve('src/_wk-init.scss');
    let sb = new Sassaby(file);

    describe('imports', () =>  {
        it('utilities', () => sb.imports('wk-sass-utils'));
    });
});
